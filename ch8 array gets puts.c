int main(int argc, char const *argv[])
{
    // gets, puts
    char str2[90];
    printf("write gets str: ");
    // scanf("%s", str2);
    gets(str2);

    printf("read puts str: ");
    puts(str2);

    return 0;
}
